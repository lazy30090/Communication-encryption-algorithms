# ==================== 核心依赖 ====================
# Flask Web框架 - 用于搭建HTTPS服务器
Flask==3.0.0

# 加密库 - 用于生成TLS证书
cryptography==41.0.7

# 图像处理库 - 用于JPEG编码
Pillow==10.1.0

# ==================== 树莓派专用说明 ====================
# PiCamera2 摄像头库通过系统包管理器安装，不通过pip
# 安装命令: sudo apt install -y python3-picamera2

# ==================== 安装步骤 ====================
# 1. 更新系统
#    sudo apt update && sudo apt upgrade -y
#
# 2. 安装系统依赖
#    sudo apt install -y python3-pip python3-picamera2 python3-pil
#
# 3. 安装Python依赖
#    pip3 install -r requirements.txt
#
# 4. 生成TLS证书
#    python3 scripts/generate_certs.py
#
# 5. 启动服务器
#    python3 server/camera_server.py
